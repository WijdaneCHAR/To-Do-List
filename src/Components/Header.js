import React from 'react';
import {Title} from '../Styling/Title/Title';
import {SubTitle} from '../Styling/Title/SubTitle'
function Header() {
  return <div>
  <Title>My Tasks</Title>
  <SubTitle>Subtracting from your list of priorities is as important as adding to it.</SubTitle>
  </div>;
}

export default Header;
